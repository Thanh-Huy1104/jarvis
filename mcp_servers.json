{
  "servers": {
    "desktop_essential": {
      "description": "Essential host system tools (filesystem, shell, docker) - Only tools that cannot be done in Python sandbox",
      "command": "uv",
      "args_template": ["run", "{script}"],
      "script": "servers/desktop/server_essential.py",
      "enabled": true
    }
  }
}
